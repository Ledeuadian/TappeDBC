export default function Logo({ className = '' }) {
  return (
    <span
      className={`inline-flex h-8 w-8 items-center justify-center rounded-lg bg-tappe-600 text-white font-bold ${className}`}
    >
      T
    </span>
  )
}
